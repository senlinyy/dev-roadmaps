```bash
az group show --name rg-devpolaris-orders-staging
```

The full resource ID proves the group lives under subscription `sub-devpolaris-training`. The tags identify it as the staging resource group for the orders API.
