```bash
az group create --name rg-devpolaris-orders-staging --location eastus --tags team=orders env=staging service=orders-api
```

The resource group now has a friendly name, a location, and inventory tags that automation and humans can use safely.
