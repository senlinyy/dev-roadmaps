Use the Azure resource group create operation.

---

The important fields are the group name, location, and tags. The tags are `team=orders`, `env=staging`, and `service=orders-api`.

---

The grader checks the simulated Azure state after your command runs.
