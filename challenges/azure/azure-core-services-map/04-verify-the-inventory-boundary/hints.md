Use Azure resource group inventory or inspection.

---

The useful evidence is the production group name, full resource ID, location, and tags.

---

If a list view is too noisy, inspect `rg-devpolaris-orders-prod` directly.
