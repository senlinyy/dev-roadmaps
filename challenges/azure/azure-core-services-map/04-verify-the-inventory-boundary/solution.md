```bash
az group show --name rg-devpolaris-orders-prod
```

The output proves the production orders API boundary is in subscription `sub-devpolaris-training`, lives in `eastus`, and carries the ownership tags needed for inventory and cost review.
