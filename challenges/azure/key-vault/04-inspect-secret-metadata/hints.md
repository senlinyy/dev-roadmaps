Inspect the Key Vault secret metadata, not the secret value.

---

The useful evidence includes vault name, secret name, enabled state, content type, tags, and version ID.

---

The vault is `kv-devpolaris-prod`; the secret is `orders-sql-connection`.
