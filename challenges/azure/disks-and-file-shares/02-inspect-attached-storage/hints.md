Start with the resource itself. The prompt gives you the names and resource groups so you do not need a separate note file.
---
After the top-level resource, inspect the child resource that owns the behavior in the scenario. For example, a container, blob, database, file share, or policy.
---
Use output evidence from the Azure CLI. The grader looks for the important names, settings, and status values, not for a prose summary.
