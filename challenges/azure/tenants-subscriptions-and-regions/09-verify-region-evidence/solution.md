```bash
az group show --name rg-devpolaris-orders-prod-eastus
```

The output proves the resource group is under subscription `sub-devpolaris-training`, placed in `eastus`, and tagged for the production orders API regional plan.
