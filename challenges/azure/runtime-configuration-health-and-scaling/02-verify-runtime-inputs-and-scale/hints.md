Start with configuration and identity because the app needs those values as soon as it starts.
---
Then inspect the release surface: revision, traffic, and ingress.
---
Finally, check scale and secret references so the team knows whether the service can handle traffic and read its private inputs.
