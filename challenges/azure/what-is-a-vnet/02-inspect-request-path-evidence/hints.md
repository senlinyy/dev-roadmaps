Use Azure network inspection operations. You need the resource group and resource name for each resource.
---
The VNet and subnet live in `rg-devpolaris-network-prod`. The SQL private endpoint lives in `rg-devpolaris-data-prod`.
---
The private DNS record uses zone `privatelink.database.windows.net` and record name `devpolaris-orders-sql`.
