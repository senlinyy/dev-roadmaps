Start with the subnet evidence for `snet-orders-api` inside `vnet-devpolaris-prod`.
---
Then inspect the routes attached to route table `rt-orders-private`.
---
Look for the route that matches `0.0.0.0/0` and check its next hop.
