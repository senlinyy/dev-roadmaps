Start with the running app, then inspect the managed identity object itself.

---

The app evidence should show a user-assigned identity. The identity evidence should show a client ID and principal ID.

---

The expected identity name is `mi-orders-api-prod`.
