Use the Azure resource group list operation to inspect the inventory.

---

The active subscription has production, staging, and shared-network groups.

---

A table output is useful here because it shows names, locations, and compact tag strings.
