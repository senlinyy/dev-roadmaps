```bash
az group create --name rg-devpolaris-orders-prod --location eastus --tags team=orders env=prod service=orders-api
```

The resource group now exists in `eastus` with ownership and environment tags attached.
