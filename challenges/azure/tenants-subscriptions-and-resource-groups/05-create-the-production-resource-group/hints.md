Use the Azure resource group create operation.

---

The required values are: name `rg-devpolaris-orders-prod`, location `eastus`, and tags `team=orders`, `env=prod`, `service=orders-api`.

---

The grader checks Azure state after the command runs. You do not need to write a report file.
