```bash
aws sts get-caller-identity
aws configure get region
```

The point is not memorizing the account number. The point is making account and Region checks a habit before a command changes real resources.
