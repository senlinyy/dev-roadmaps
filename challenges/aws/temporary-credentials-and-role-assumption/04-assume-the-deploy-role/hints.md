The role ARN is `arn:aws:iam::123456789012:role/devpolaris-github-deploy-role`, and the session name is `github-actions-deploy`.

---

Use STS to confirm the current caller before assuming the role.

---

Use STS role assumption with the provided role ARN and session name.
