Start with the service. The service output shows deployments, running counts, and recent events.

---

Use the service name to list tasks for the cluster. Then inspect the returned task ARNs.

---

The task definition output is where secret references and the execution role are visible.
