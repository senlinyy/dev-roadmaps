Check the service first. The service should show revision 57 as the active task definition with no pending tasks.

---

Target health should show all registered targets as healthy. Alarm state should be `OK`.

---

Use the log group and a rollback-related filter to confirm that application evidence matches the AWS service state.
