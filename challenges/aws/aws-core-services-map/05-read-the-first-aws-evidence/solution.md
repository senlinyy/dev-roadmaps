```bash
aws sts get-caller-identity
aws configure get region
aws s3api list-buckets
```

This does not prove the app is correct. It gives you the first layer of evidence before you jump deeper into code, IAM, networking, or logs.
