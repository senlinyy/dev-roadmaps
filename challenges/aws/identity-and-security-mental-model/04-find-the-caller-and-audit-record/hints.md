The event name is `GetSecretValue`, and the failed secret path is `/devpolaris/orders-api/prod/database-url`.

---

Use STS to answer "who is this session?"

---

Use CloudTrail's event lookup operation and filter for the `GetSecretValue` event name to find the audit record.
