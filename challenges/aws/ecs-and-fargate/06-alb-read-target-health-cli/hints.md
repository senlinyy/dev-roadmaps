The load balancer is `devpolaris-orders-alb`, the listener port is `443`, the target group is `devpolaris-orders-api-tg`, and the health check path is `/health`.

---

Use ELBv2 to inspect load balancer `devpolaris-orders-alb`, then inspect the listeners attached to its ARN.

---

Use ELBv2 target group and target health inspection for `devpolaris-orders-api-tg`.
