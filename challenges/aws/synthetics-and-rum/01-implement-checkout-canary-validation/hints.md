Validate transport evidence before parsing or trusting business fields. Treat the content type as case-insensitive and allow a charset suffix.
---
The function should throw for invalid responses and return checkoutId plus elapsedMs for a valid one.
