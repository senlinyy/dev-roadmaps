Use the S3 API command that lists objects inside a bucket.

---

The bucket is `devpolaris-orders-exports-prod`.

---

Focus the object listing on the `orders-api/daily/2026/05/` path so the output stays focused on the May daily exports.
