AttributeDefinitions contains only attributes used by the table or index key schemas.
---
A global secondary index needs its own KeySchema and Projection. Use PAY_PER_REQUEST so no provisioned throughput object is needed.
