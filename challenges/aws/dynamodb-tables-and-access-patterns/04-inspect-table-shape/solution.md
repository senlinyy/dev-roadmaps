```bash
aws dynamodb describe-table --table-name devpolaris-checkout-state
```

The table shape tells you how the application is expected to read and write items. The key schema is not decoration; it is the main access pattern.
