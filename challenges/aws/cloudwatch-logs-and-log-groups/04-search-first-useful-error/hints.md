Start by listing log groups with the orders API name as the prefix.

---

Then filter events inside `/aws/ecs/devpolaris-orders-api`.

---

Use the `ERROR` signal to narrow the output, then confirm the event belongs to correlation ID `req-7b91`.
