Inspect the ECS service first. The service is `devpolaris-orders-api` in cluster `devpolaris-orders-prod`.

---

Then inspect target health for the target group ARN in the prompt.

---

Running ECS tasks and healthy load balancer targets are related, but they are not the same signal.
