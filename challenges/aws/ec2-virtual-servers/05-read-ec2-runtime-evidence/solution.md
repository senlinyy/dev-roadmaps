```bash
aws ec2 describe-instances --instance-ids i-orders-api-01
```

For this checkpoint, stay with the EC2 evidence: AWS can show whether the server exists, where it runs, and which network shape it has.
