The three security groups are `sg-orders-alb`, `sg-orders-api`, and `sg-orders-db`.

---

Use EC2 security group inspection for `sg-orders-alb`, `sg-orders-api`, and `sg-orders-db`.

---

Read each `IpPermissions` block as "which source can reach which port?"
