Inspect route table `rtb-private-orders`, NAT gateway `nat-orders-a`, and endpoints `vpce-secretsmanager` and `vpce-logs`.

---

Use EC2 route table and NAT gateway inspection for `rtb-private-orders` and `nat-orders-a`.

---

Use EC2 VPC endpoint inspection for `vpce-secretsmanager` and `vpce-logs`.
