Inspect `devpolaris-orders-alb`, subnets `subnet-public-a` and `subnet-private-a`, and security groups `sg-orders-alb` and `sg-orders-api`.

---

Use ELBv2 load balancer inspection for `devpolaris-orders-alb` to understand the front door.

---

Use EC2 subnet and security group inspection for `subnet-public-a`, `subnet-private-a`, `sg-orders-alb`, and `sg-orders-api` for the next pieces.
