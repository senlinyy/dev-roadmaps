The route tables are `rtb-public-orders` and `rtb-private-orders`; the target resources are `igw-orders-prod` and `nat-orders-a`.

---

Use EC2 route table inspection for `rtb-public-orders` and `rtb-private-orders`.

---

Use EC2 internet gateway and NAT gateway inspection for `igw-orders-prod` and `nat-orders-a`.
