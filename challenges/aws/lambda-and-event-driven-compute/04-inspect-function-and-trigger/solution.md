```bash
aws lambda get-function-configuration --function-name devpolaris-receipt-email
aws lambda list-event-source-mappings --function-name devpolaris-receipt-email
```

For Lambda, the function settings describe one run. The event source mapping explains why that run happens.
