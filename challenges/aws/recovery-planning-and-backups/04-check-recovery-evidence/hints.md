Database recovery evidence lives with RDS snapshots for `devpolaris-orders-prod`.

---

Object recovery evidence lives with versioning on bucket `devpolaris-orders-exports-prod`.

---

Look for available, encrypted database snapshots and enabled object versioning.
