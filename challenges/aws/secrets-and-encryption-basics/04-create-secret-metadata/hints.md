The secret name is `/devpolaris/orders-api/prod/database-url`, the practice value is `postgres://orders-prod-redacted`, and the tags are `service=devpolaris-orders-api` and `env=prod`.

---

Use Secrets Manager's secret creation operation with the provided name, practice value, and both tags.

---

Use Secrets Manager's metadata inspection operation for `/devpolaris/orders-api/prod/database-url` so you can verify the secret without printing its value.
