```bash
aws cloudwatch describe-alarms --alarm-names devpolaris-orders-api-5xx-high
```

An alarm is useful because it connects a metric to a human action. The state and threshold tell you whether the signal is currently asking for attention.
