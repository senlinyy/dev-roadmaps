Use the RDS command that describes DB instances.

---

The instance identifier is `devpolaris-orders-prod`.

---

You should see the endpoint, encryption, Multi-AZ, public access, and backup retention fields in the output.
