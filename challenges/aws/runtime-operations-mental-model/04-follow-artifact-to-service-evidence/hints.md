Start from the container registry evidence for repository `devpolaris-orders-api`.

---

Then inspect task definition `devpolaris-orders-api:42`.

---

Finally inspect service `devpolaris-orders-api` in cluster `devpolaris-orders-prod`.
