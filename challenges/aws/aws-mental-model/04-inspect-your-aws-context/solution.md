```bash
aws sts get-caller-identity
aws configure get region
```

The first command shows the current AWS account and caller ARN. The second command shows the Region AWS CLI commands will use when a command does not specify one directly.
