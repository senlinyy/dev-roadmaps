```bash
aws iam get-role --role-name devpolaris-orders-api-prod-task-role
aws iam list-attached-role-policies --role-name devpolaris-orders-api-prod-task-role
```

The role tells you who can use it. The attached policy tells you what that role is meant to do once the app is running.
