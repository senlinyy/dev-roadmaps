The exact role name is `devpolaris-orders-api-prod-task-role`.

---

Use IAM's role inspection operation for `devpolaris-orders-api-prod-task-role` to confirm the role metadata.

---

Then use IAM's attached policy listing operation for the same role to see which policy is attached.
