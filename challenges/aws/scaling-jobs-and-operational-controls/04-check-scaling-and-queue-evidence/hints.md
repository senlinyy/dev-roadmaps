For scaling, inspect the Application Auto Scaling target for the ECS service resource ID.

---

For backlog, inspect attributes on the SQS queue URL from the prompt.

---

Capacity bounds and queue depth answer different questions. Read both before changing worker count.
