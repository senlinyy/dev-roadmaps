---
title: "Design Service Inputs"
sectionSlug: designing-variables-for-production
order: 1
revision: 2
---

Turn a hard-coded service configuration into a reusable deployment. Define safe defaults, keep environment-specific values in group variables, and render both through a template with a mandatory API endpoint.

Work across the provided files as needed. The grader checks the resulting Ansible project, including relationships between files, rather than matching a sample answer.
