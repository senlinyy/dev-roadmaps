1. The play should become a small caller of `orders_web`.
2. Keep role-owned files under the role directory so references remain local.
3. A template notification must resolve to a handler in the same role.
