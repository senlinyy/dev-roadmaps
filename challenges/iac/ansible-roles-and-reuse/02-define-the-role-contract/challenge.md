---
title: "Define the Role Contract"
sectionSlug: validating-inputs-early
order: 2
revision: 2
---

Give the role an explicit argument contract for its required upstream URL and optional port. Validate the URL before any service task, while keeping the default port in one place.

Work across the provided files as needed. The grader checks the resulting Ansible project, including relationships between files, rather than matching a sample answer.
