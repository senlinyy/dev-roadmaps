1. Create one file task per distinct ownership contract.
2. Directory state should be `directory`; the environment file can use `touch`.
3. Preserve times on the touch task so repeated runs do not report spurious changes.
