1. Use template when content depends on variables.
2. The validate command must include `%s` for Ansible's candidate file.
3. Use default only where a safe default exists; require the upstream value.
