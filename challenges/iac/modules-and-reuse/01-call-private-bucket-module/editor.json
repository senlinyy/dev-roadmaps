{
  "language": "hcl",
  "completionProfile": "terraform",
  "activeFile": "main.tf",
  "files": [
    {
      "filename": "main.tf",
      "initialValue": "terraform {\n  required_version = \">= 1.6.0\"\n}\n\nprovider \"aws\" {\n  region = \"eu-west-2\"\n}\n"
    }
  ],
  "grader": {
    "strategy": "regex-all",
    "expected": [
      {
        "file": "main.tf",
        "pattern": "^\\s*module\\s+\"invoice_bucket\"\\s*\\{[^}]*source\\s*=\\s*\"\\.\\./\\.\\./modules/private-bucket\"[^}]*}",
        "label": "invoice_bucket module uses the local private bucket source"
      },
      {
        "file": "main.tf",
        "pattern": "^\\s*module\\s+\"invoice_bucket\"\\s*\\{[^}]*bucket_name\\s*=\\s*\"dp-orders-invoices-prod\"[^}]*}",
        "label": "invoice_bucket passes the production invoice bucket name"
      },
      {
        "file": "main.tf",
        "pattern": "^\\s*module\\s+\"invoice_bucket\"\\s*\\{[^}]*service\\s*=\\s*\"orders-api\"[^}]*}",
        "label": "invoice_bucket passes the service name"
      },
      {
        "file": "main.tf",
        "pattern": "^\\s*module\\s+\"invoice_bucket\"\\s*\\{[^}]*environment\\s*=\\s*\"prod\"[^}]*}",
        "label": "invoice_bucket passes the production environment"
      },
      {
        "file": "main.tf",
        "pattern": "^\\s*module\\s+\"invoice_bucket\"\\s*\\{[^}]*owner\\s*=\\s*\"platform\"[^}]*}",
        "label": "invoice_bucket passes the platform owner"
      },
      {
        "file": "main.tf",
        "pattern": "^\\s*module\\s+\"invoice_bucket\"\\s*\\{[^}]*versioning_enabled\\s*=\\s*true[^}]*}",
        "label": "invoice_bucket enables versioning"
      }
    ]
  }
}
