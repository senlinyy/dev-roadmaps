---
title: "Build a First Convergent Playbook"
sectionSlug: playbooks-plays-and-tasks
order: 1
revision: 2
---

The orders team has two freshly provisioned web hosts. Create a play that targets the inventory group, escalates privileges, installs nginx, and ensures the service is enabled and running. Use declarative modules so a second run can settle with no unnecessary changes.

Work across the provided files as needed. The grader checks the resulting Ansible project, including relationships between files, rather than matching a sample answer.
