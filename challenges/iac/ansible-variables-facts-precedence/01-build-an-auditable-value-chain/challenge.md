---
title: "Build an Auditable Value Chain"
sectionSlug: auditing-the-winning-value
order: 1
revision: 2
---

Model a deployment where all hosts have a safe worker default, production raises it, and one canary host temporarily lowers it. Add a read-only preflight task that exposes the resolved value before any change.

Work across the provided files as needed. The grader checks the resulting Ansible project, including relationships between files, rather than matching a sample answer.
