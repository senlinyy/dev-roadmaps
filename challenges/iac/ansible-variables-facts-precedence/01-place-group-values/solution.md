### group_vars/orders_web.yml

```yaml
orders_service_name: devpolaris-orders-api
orders_api_port: 8080
orders_owner: devpolaris
```

These values apply consistently to the inventory group while remaining available for higher-precedence overrides when a host needs an exception.
