1. Begin with `ansible-inventory -i inventory.ini --graph`.
2. The playbook command has separate flags for syntax, hosts, tasks, and check mode.
3. Use `--limit web-01` on the preview so the final target set is one host.
