---
title: "Support a Mixed Linux Fleet"
sectionSlug: a-mixed-linux-fleet-example
order: 1
revision: 2
---

The orders fleet includes Debian and Red Hat hosts. Install nginx with the native package module for each family, then manage the correct service name without relying on shell probes.

Work across the provided files as needed. The grader checks the resulting Ansible project, including relationships between files, rather than matching a sample answer.
