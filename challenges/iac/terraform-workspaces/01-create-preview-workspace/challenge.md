---
title: "Create and Verify a Preview Workspace"
sectionSlug: creating-and-selecting-workspaces
order: 1
revision: 2
---

The initialized training root has default, staging, and production workspaces. Create a new workspace named `preview-pr-482`, verify Terraform selected it, and leave production untouched.

Your job:

1. **List** the existing workspace names.
2. **Create** `preview-pr-482`.
3. **Show** the current workspace and confirm it is the new preview workspace.

The grader checks the command workflow and resulting Terraform state, not a prose explanation.
