---
title: "Separate Connection Settings"
sectionSlug: groups-children-and-useful-slices
order: 2
revision: 2
---

The inventory works, but connection policy is duplicated on every host. Move shared SSH settings to group variables while preserving host-specific addresses and the production hierarchy.

Work across the provided files as needed. The grader checks the resulting Ansible project, including relationships between files, rather than matching a sample answer.
