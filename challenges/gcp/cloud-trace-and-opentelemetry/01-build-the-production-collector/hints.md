The OTLP receiver can expose both `grpc` and `http` under its protocols map.

Resource attributes are applied before batching, and the trace pipeline refers to component names rather than repeating their configuration.
