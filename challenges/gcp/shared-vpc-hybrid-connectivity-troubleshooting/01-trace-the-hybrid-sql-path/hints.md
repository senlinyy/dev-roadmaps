Start by reading the saved test definition, then execute that named test in the host project.

The first useful conclusion is the first control that blocks the packet, not a guess based only on the destination application.
