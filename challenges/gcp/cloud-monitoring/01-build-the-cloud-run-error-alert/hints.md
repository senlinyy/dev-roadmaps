Use the Cloud Run request-count metric and filter both the service name and project ID. Add the HTTP response-code class filter for 5xx traffic.

The alignment period and violation duration are both five minutes, expressed in seconds.
