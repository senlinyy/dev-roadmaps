Health and session continuity are separate checks.

---

A shared session backend is a runtime setting; a release codec is immutable evidence.

---

Fix editable drift, but stop a candidate whose bytes cannot read existing sessions.
