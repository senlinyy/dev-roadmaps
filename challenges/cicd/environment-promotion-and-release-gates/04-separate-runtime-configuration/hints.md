The config map, secret references and artifact bytes have separate responsibilities.

---

The registry already contains the intended executable; a per-environment rebuild is unnecessary.

---

Healthy staging does not grant deployment authority by itself.
