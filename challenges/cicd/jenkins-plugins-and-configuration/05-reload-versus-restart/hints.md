Inspect the earliest failed operation and compare its available inputs with the fixed scenario. Distinguish local files and authored settings from the state actually in use.

---

The plan treats a YAML reload as if it replaced plugin classes. Snapshots distinguish authored inputs from loaded process state.

---

Complete the lab-2 manifest. Apply the YAML reload to show its limited effect, then build and boot the new image, smoke-test that process, and promote only its tested digest.
