CasC YAML mirrors Jenkins configuration sections. Add controller execution policy, local authentication, and authorization under the existing top-level Jenkins mapping while preserving the seeded system message.
