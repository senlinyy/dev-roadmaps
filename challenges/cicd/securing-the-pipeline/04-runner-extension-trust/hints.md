Inspect pool trust and network reach separately from job names.

---

An extension executes with the job's authority; a wildcard revision leaves that authority available to changed code.

---

Keep public validation disposable and restrict the protected release path by workload, pool eligibility, and immutable extension identity.
