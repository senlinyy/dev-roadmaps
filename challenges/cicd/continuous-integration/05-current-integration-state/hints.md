Compare the revision in each check report with the revision the merge policy accepts.

---

A complete check list is insufficient if the workflow validates a different source state.

---

Run the three named checks on candidate, then evaluate a current-only merge policy downstream.
