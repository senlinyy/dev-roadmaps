Inspect resolved dependency versions in both initial runs. The manifest describes an allowed range; which supplied record identifies the approved selection?

---

Restoring the archived lock and choosing an installation policy are separate decisions. An existing lockfile alone does not satisfy this simulator's locked-install requirement.

---

A stable installation still needs validation. Run the test script before building so the compatibility fixture and independent test-failure case can reject an unsafe candidate.
