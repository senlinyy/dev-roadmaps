Masking protects values Jenkins sees through a credential binding. Search the log for the deploy token prefix to find the cleartext leak, then search for the credential startup line to prove which binding should have protected the run.
