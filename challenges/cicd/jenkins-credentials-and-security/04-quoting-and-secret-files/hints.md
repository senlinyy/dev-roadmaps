Inspect the earliest failed operation and compare its available inputs with the fixed scenario. Distinguish local files and authored settings from the state actually in use.

---

The current workflow binds inside a browsable subdirectory, interpolates the sensitive path into Groovy metadata, and archives broadly. Masking the console does not repair those exposure paths.

---

Validate and build before the release allocation, then transfer the application. Bind registry-file as PUBLISH_CONFIG before entering release, use shell-side expansion for the publisher's file argument, and archive only dist/app.json after leaving the credential scope. Keep PRs outside the release path.
