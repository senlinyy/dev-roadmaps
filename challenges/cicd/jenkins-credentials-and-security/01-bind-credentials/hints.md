Bind both deploy credentials inside the Deploy stage for the smallest useful scope. Use the credential IDs named in the prompt, expose them as environment variables only inside the binding closure, and keep the Build stage unchanged.
