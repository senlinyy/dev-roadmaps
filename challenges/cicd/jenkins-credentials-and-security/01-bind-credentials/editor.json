{
  "language": "groovy",
  "filename": "Jenkinsfile",
  "completionProfile": "jenkinsfile",
  "initialValue": "pipeline {\n  agent any\n\n  environment {\n    AWS_ACCESS_KEY_ID = 'AKIAIOSFODNN7EXAMPLE'\n    AWS_SECRET_ACCESS_KEY = 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY'\n    SLACK_WEBHOOK = 'https://hooks.slack.com/services/T000/B000/XXXXX'\n  }\n\n  stages {\n    stage('Build') {\n      steps {\n        sh 'mvn -B package'\n      }\n    }\n    stage('Deploy') {\n      steps {\n        sh '''\n          aws s3 cp target/orders.jar s3://devpolaris-artifacts/\n          curl -X POST -d 'deployed' $SLACK_WEBHOOK\n        '''\n      }\n    }\n  }\n}\n",
  "grader": {
    "strategy": "regex-all",
    "expected": [
      {
        "pattern": "(?<!w)withCredentials\\s*\\(",
        "label": "Deploy stage uses withCredentials(...)"
      },
      {
        "pattern": "credentialsId\\s*:\\s*['\"]devpolaris-aws-deploy['\"]",
        "label": "withCredentials references credentialsId 'devpolaris-aws-deploy'"
      },
      {
        "pattern": "credentialsId\\s*:\\s*['\"]devpolaris-slack-webhook['\"]",
        "label": "withCredentials references credentialsId 'devpolaris-slack-webhook'"
      },
      {
        "pattern": "variable\\s*:\\s*['\"]SLACK_WEBHOOK['\"]",
        "label": "Slack webhook bound to variable SLACK_WEBHOOK"
      },
      {
        "pattern": "AWS_ACCESS_KEY_ID",
        "label": "AWS access key id variable is referenced"
      },
      {
        "pattern": "AWS_SECRET_ACCESS_KEY",
        "label": "AWS secret access key variable is referenced"
      },
      {
        "pattern": "mvn\\s+-B\\s+package",
        "label": "Build stage still runs mvn -B package"
      },
      {
        "pattern": "AKIAIOSFODNN7EXAMPLE",
        "label": "the inline AWS access key literal is removed",
        "flags": "ms",
        "negate": true
      }
    ]
  }
}
