Read the emitted OIDC subject rather than assuming it includes an environment.

---

A workflow without an environment presents branch-based identity in this lab.

---

Match the exact main-branch subject and STS audience, and narrow the release job’s token permissions.
