Production authorization and irreversible-change authorization protect different transitions.

---

A post-contract failure cannot safely reuse the pre-contract rollback branch.

---

Keep the committed checkpoints and hand the incident to an accountable owner rather than pretending a binary revert undoes data changes.
