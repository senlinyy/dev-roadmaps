Compare cache state with the resolved installation in every run.

---

A compatible cache changes acquisition cost, not the manifest/lock contract.

---

Include all three compatibility inputs in the cache key and perform locked installation on cold and warm paths.
