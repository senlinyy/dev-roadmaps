Expand the supported OS/runtime combinations before changing YAML.

---

A matrix dependency must represent the entire compatibility result.

---

Exclude Windows/22, disable fail-fast for complete evidence, and make packaging need the matrix job.
