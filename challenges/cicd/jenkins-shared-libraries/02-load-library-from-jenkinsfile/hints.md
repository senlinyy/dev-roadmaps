Inspect the earliest failed operation and compare its available inputs with the fixed scenario. Distinguish local files and authored settings from the state actually in use.

---

A moving reference selects a breaking API, while the wrong registered name prevents loading. Fixing only the annotation leaves untested caller behavior.

---

Align library-settings.yaml with company-pipeline, make v1.4.2 the stable default, and select that reviewed version in Jenkinsfile. Invoke its command interface for both quality checks and keep packaging behind them. Do not edit the supplied snapshots.
