Inspect the earliest failed operation and compare its available inputs with the fixed scenario. Distinguish local files and authored settings from the state actually in use.

---

The wrapper ignores its caller and builds an application instead. Sharing it would spread the defect.

---

Repair the global step to prepare its allocation and run the command supplied by the caller. Replace duplicated mechanics in Jenkinsfile with named command inputs for lint and unit tests. Keep packaging downstream, outside the shared check API.
