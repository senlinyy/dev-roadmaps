---
title: "The Library That Broke Prod"
sectionSlug: the-day-the-library-broke-production
order: 5
---

A real outage. The `devpolaris-pipeline` shared library shipped v1.5.0 to fix a logging bug. Within four minutes of the merge, every production deploy pipeline that pinned `@Library('devpolaris-pipeline@main') _` started failing the same way. Each scenario asks about a step in the response or the prevention. Pick the most accurate answer.
