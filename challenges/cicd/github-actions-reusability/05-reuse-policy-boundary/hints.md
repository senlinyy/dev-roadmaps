Inspect the release interface and the caller’s secret mapping.

---

Shared mechanism must not silently select production or inherit unrelated authority.

---

Pass staging and its one named token explicitly; keep the call behind the packaged artifact.
