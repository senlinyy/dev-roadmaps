Locate both the action metadata and the backend package manifest.

---

An action path and an application working directory are different locations.

---

Call the actual local action, pass backend separately, and route the runtime input into setup.
