Inspect embeddedConfig and each environment's configuration contract.

---

The build should not decide which database, secret, or feature exposure production uses.

---

Supply configuration at deployment, verify staging, and gate production on that verified path.
