Compare approval artifact, destination, actor, and expiry with the release candidate.

---

The approval policy validates the decision; the deployment policy must require it, and the graph must require staging.

---

Bind the approval to the candidate and place production behind staging verification. Test each invalid approval separately.
