{
  "language": "yaml",
  "filename": ".github/workflows/scan.yml",
  "completionProfile": "github-actions",
  "initialValue": "name: Security Scan\non: [push]\n\njobs:\n  scan:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      # Add a step that runs inside a Docker container",
  "grader": {
    "strategy": "yaml-paths",
    "expected": [
      {
        "path": ["jobs", "scan", "steps"],
        "containsObject": {
          "uses": "docker://alpine:3.20",
          "with": {
            "entrypoint": "/bin/sh",
            "args": "-c \"echo 'Scanning...'\""
          }
        },
        "label": "scan job runs the stated command inside Alpine 3.20"
      }
    ]
  }
}
