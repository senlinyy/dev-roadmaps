Compare the cache key and install evidence across cold, warm, and stale cases.

---

A cache accelerates preparation; it must not decide whether dependencies are installed.

---

Key npm downloads by OS/runtime/lockfile and run npm ci after every lookup.
