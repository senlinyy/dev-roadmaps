A machine average can hide an uneven CPU distribution. Compare a batch process overview with per-CPU accounting.

---

mpstat's processor selector can include all CPUs; give both an interval and a count for bounded sampling.
