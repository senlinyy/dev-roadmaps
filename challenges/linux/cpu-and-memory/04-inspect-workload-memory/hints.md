Use the service manager to locate the current process. Compare its RSS with virtual size before looking at proportional and shared memory.

---

The status file provides VmRSS and VmSize. The smaps_rollup file adds PSS and shared/private accounting; use authorized elevated access when inspecting the app account's protected rollup.
