A workload can reach its own memory boundary before the whole host runs out of RAM. Compare loaded configuration with the kernel event's scope.

---

Look for a memory-cgroup constraint naming orders.service, then compare it with the unit's effective MemoryMax and Result.
