Separate swap capacity from movement. Compare the interval samples with pressure stalls and resident-memory ownership.

---

After ranking processes, use the leading PID's status file to inspect VmSwap. This is an investigation, not permission to terminate the job.
