```bash
$ cat /proc/loadavg
$ grep -c processor /proc/cpuinfo
$ cat /proc/uptime
```

The 1-minute load is 4.82 on 2 cores, giving a per-core load of 2.41: the system is overloaded. The uptime is 86412 seconds, roughly 24 hours.
