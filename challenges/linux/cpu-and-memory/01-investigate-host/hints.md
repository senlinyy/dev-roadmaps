Compare load with capacity, then check whether the current samples show runnable work or blocked work. The process table supplies ownership.

---

The first vmstat report contains since-boot rates. Request enough reports to obtain two later interval samples.
