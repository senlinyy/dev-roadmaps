Set the initial priority while launching, then use a running-process priority tool for the later adjustment.

---

The shell exposes the last background PID through `$!`. CPU nice values and I/O scheduling priority require separate checks.

