A successful signal command means the signal was sent, not that the process exited.

---

Advance the lab clock with a foreground sleep, then inspect the same PID. Use force only after that evidence.

