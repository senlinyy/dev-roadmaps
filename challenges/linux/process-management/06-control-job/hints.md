The background marker returns the prompt immediately. Use the shell job number, not the PID, for foreground and background controls.

---

Suspending is different from terminating. The final process check can use the last background PID stored in `$!`.

