Find candidates by command name, but choose the target using its owner, parent, and full path.

---

Compare the old-release working directory and session cgroup with the intended service. A final snapshot can check both PIDs together.

