TERM requests cleanup; KILL bypasses it. Verify the target identity first.

---

In this lab, foreground sleep advances simulated time immediately. After the drain, inspect the PID again and check the lock path.

