```bash
vim routing.conf
```

In Normal mode, press `6G`, `gg`, `2j`, `0`, then `w`. The cursor should be at line 3, column 9. Enter `:q` and press `Enter`.

```bash
cat routing.conf
```

Navigation leaves both the buffer and saved file unchanged.
