```bash
vim targets.conf
```

In Normal mode, press `2G`, `yy`, `p`, `u`, then `Ctrl+r`. Enter `:wq` and press `Enter`.

```bash
cat targets.conf
```

The final file has two target lines. Undo and redo exercise recovery without losing the intended change.
