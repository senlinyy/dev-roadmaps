Yank copies into a Vim register, and put inserts that register. Linewise yanks preserve whole lines.

---

Undo must remove the inserted copy; redo must restore it. Redo in Vim uses Ctrl+r, not the browser refresh shortcut.
