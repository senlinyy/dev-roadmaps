Press `i` to enter Insert mode. You will see `-- INSERT --` at the bottom. Now your keystrokes type text. Press `Esc` when done editing.

---

To save: type `:wq` (colon enters Command mode, `w` writes, `q` quits). If you made a mistake, `:q!` quits without saving so you can start over.
