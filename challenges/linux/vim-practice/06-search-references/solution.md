```bash
vim upstreams.conf
```

Type `/api.internal` and press `Enter`. Press `n`, then `N`. Type `?api.internal` and press `Enter`. Enter `:q` and press `Enter`.

```bash
cat upstreams.conf
```

Searching moves the cursor, not the file contents.
