Search forward and search backward begin with different punctuation. Enter accepts the search.

---

After accepting a search, next-match and previous-match keys reuse it. A search that finds nothing does not demonstrate navigation.
