A substitution has a range, pattern, replacement, and flags. File-wide range and every-match-per-line are separate choices.

---

Add confirmation so you can accept active entries and skip the comment. A blind global replacement changes too much.
