Navigation commands work in Normal mode. If you see `-- INSERT --` at the bottom, press `Esc` first.

---

Try `G` to jump to the bottom of the file, then `gg` to jump back to the top. Use `:15` followed by Enter to jump to a specific line number.
