Vim starts in Normal mode. Insert mode types text; Escape returns to Normal mode.

---

Reach the value after the equals sign. Insert mode also supports Delete to remove the old characters. Writing and quitting are separate operations, or you can combine them.
