All editing commands work in Normal mode, not Insert mode. If you see `-- INSERT --` at the bottom, press `Esc` first.

---

`dd` deletes a line, `yy` copies it, `p` pastes it below. Use a number prefix like `3yy` to copy 3 lines at once. Press `u` to undo if something goes wrong.
