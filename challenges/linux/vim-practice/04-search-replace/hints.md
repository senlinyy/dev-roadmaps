Type `/` followed by a word to search forward. Press `n` to jump to the next match. Press `N` to go backward.

---

The substitution syntax is `:%s/old/new/g`. The `%` means all lines, `g` means all matches per line. Without `g`, only the first match on each line is replaced.
