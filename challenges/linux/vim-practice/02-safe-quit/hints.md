The buffer is your working copy. After a successful write, later edits can still be unsaved.

---

An ordinary quit should show E37 when the buffer differs from the saved file. A forced quit discards only changes since the last write, not everything from the session.
