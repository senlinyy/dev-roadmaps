```bash
export PATH="$HOME/tools:$PATH"
command -v release-status
release-status
```

Prepending the tools directory makes the project command discoverable without removing the standard command locations.
