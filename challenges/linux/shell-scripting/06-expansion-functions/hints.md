Run `vim pathutil.sh`, press `i`, type the script, then `Esc` and `:wq`. The `%` operator strips from the end, and `##` strips from the beginning.

---

For the function script, run `vim greet.sh`. The structure is: define `greet()` with curly braces, use `local name="$1"` inside, and call `greet DevPolaris` after the definition.
