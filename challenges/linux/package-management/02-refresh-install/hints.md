Refreshing package lists and upgrading installed packages are separate APT operations.

---

After refreshing the local catalog, request only the missing package.

---

APT can list packages whose status is installed. Use that inventory as the final evidence.
