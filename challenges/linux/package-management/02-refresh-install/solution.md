```bash
sudo apt update
sudo apt install curl
apt list --installed
```

The first command refreshes metadata only. The install then records `curl` version `8.5.0-2ubuntu10.6`, and the final inventory confirms the managed result.
