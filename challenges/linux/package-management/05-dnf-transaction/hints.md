DNF has a check operation that lists updates without applying them. It may return status 100 when updates exist.

---

Inspect package information and repository inventory separately so the source is explicit.

---

RPM's file query accepts an installed filesystem path and returns the owning package build.
