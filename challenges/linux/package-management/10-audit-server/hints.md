Begin with the Debian installed-package inventory. It should show the package name, version, architecture, and description.

---

Query the exact `/usr/bin/curl` path for ownership, then use command resolution to locate `backupctl`.

---

Run the same ownership query against the resolved internal binary even if you expect it to fail. Finish with APT's policy view for OpenSSL.
