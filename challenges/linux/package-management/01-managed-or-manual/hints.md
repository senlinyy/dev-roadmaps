The Debian package database can search for the owner of an exact filesystem path.

---

Once you know the package name, ask APT to show its metadata.

---

Use shell command resolution to locate the internal utility. Files under `/usr/local` are commonly installed outside the distribution package database.
