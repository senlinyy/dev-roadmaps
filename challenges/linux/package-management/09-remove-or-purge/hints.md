An ordinary APT removal deletes packaged program files but intentionally preserves configuration.

---

List the service configuration directory before purging so you capture the difference.

---

After the purge, search that directory for regular files. An empty result is the expected final evidence.
