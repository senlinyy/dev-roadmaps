APT's policy view shows installed and candidate versions together with repository priority.

---

The Debian package database, rather than the shell path lookup, records file ownership.

---

Use the APT list filter that previews packages with newer candidates.
