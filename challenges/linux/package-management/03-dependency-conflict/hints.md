Start by requesting the application normally. The error identifies both the required dependency version and the held version.

---

APT policy output separates the installed version from the candidate version.

---

The tool used to create a hold can also remove it. Retry the original install only after the dependency is free to move.
