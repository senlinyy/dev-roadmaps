Use the policy view before changing package selection state.

---

APT tracks explicit package holds separately from installed versions.

---

After the normal upgrade, list the holds. The protected package should remain old while `curl` reaches its candidate.
