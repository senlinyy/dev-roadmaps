Use the APT upgrade preview first so the old and candidate versions are visible.

---

APT can limit an upgrade to one named package.

---

After the files change, use the restart inspection tool to find processes still using superseded libraries.
