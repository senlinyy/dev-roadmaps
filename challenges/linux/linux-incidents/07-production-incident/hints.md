A healthy filesystem does not imply a healthy service. Compare the journal and execution identity with both the saved content and file metadata.

---

There is more than one fault. Once the service can read the file, validation can reveal the remaining problem. Re-run validation as the service user before the final restart.
