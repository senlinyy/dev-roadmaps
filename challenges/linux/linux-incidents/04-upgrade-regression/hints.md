Compare the installed package with the available version table, then use the service journal to decide which release is compatible.

---

Installing a named version and holding a package are separate operations. A rollback alone does not prevent the next upgrade.
