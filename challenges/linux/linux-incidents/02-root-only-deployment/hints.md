An absolute path can work even when a command name cannot be resolved. Compare the search path with the installation directory.

---

Extend and export the existing search path. Changing mode bits will not add a directory to command lookup.
