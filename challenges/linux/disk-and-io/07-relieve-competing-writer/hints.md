Identify the export from the process listing before selecting a PID. Process I/O counters are cumulative, so compare readings over time.

---

The open-file path links the writer to storage. Only the authored export exit changes this fixture's device performance.
