Growing the external device does not expand the partition or filesystem. Inspect each actual boundary.

---

The partition operation targets a disk plus partition number. XFS online growth targets the mounted filesystem.
