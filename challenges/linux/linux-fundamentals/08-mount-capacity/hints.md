A path can belong to a more specific mounted filesystem even though it appears below the root tree.

---

Use the mount-inspection command with the application path, then scope the filesystem-capacity command to the same path.

---

The directory-usage command can report human-readable totals with a maximum depth of one.
