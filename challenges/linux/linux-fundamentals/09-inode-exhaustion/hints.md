The ordinary filesystem-capacity view measures blocks, not the number of file records.

---

The same capacity command has an inode-reporting option.

---

Search only for regular files below the session directory and pipe the resulting paths into a line counter.
