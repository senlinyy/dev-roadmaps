```bash
command -v python
type -a python
command -v ordersctl
```

- `/usr/local/bin` appears before `/usr/bin` in this shell's `PATH`.
- `type -a` shows both reachable Python executables in resolution order.
