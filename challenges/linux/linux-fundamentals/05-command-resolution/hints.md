The shell searches the directories in `PATH` from left to right.

---

Use the shell command that reports the executable it would select, rather than a whole-filesystem search.

---

The shell's type-inspection builtin has an option that displays every matching executable instead of stopping at the first.
