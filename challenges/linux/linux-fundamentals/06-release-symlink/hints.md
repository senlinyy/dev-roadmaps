A symbolic link stores another pathname rather than copying the target directory.

---

Use the link-reading command to inspect the current target before changing it.

---

The link-creation command needs symbolic and replacement behaviour because `current` already exists. Canonical link resolution verifies the complete destination afterward.
