Start with the command that prints a process identity, including supplementary groups.

---

A long listing shows symbolic permissions and named ownership. Point it at the file rather than listing the whole tree.

---

Use the metadata command that reports an `Access` line with an octal mode.
