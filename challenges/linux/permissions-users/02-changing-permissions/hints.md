Use `chmod u+x file` to add execute permission for the user (owner) only.

---

Octal 600 means `rw-------`. Only the owner can read and write the file.

---

Octal 755 means `rwxr-xr-x`. Owner gets full access, group and others get read and execute (traverse for directories).
