Use `ls -l <directory>` to see the permission string, owner, group, size, and name of each file.

---

The permission string is 10 characters: file type + 3 triplets of rwx (owner, group, others). A dash means that permission is not granted.

---

`cat` displays the contents of a file. Use it to read the report.
