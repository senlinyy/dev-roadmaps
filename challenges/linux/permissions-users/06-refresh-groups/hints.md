Account membership and a running process's supplementary groups are related but not identical snapshots.

---

The socket already grants read and write to `docker`. Do not widen its mode.

---

Use the command that starts a shell context with a named group, then repeat both the identity and access checks.
