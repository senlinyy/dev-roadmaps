A readable final file is not enough. Every parent directory must grant execute permission to the service's matching class.

---

Use a pathname inspection command to find the first directory where `app` loses traversal.

---

Assign the blocking directory to the service's shared group, then grant owner full access and group read plus traversal without opening it to others.
