The `id` command shows your numeric UID, GID, and all group memberships.

---

Use `chown user:group file` to change both owner and group at once. The colon separates the user from the group.

---

Use `chgrp group file` to change only the group of a file without affecting the owner.
