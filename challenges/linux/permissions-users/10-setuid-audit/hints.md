Setuid is the octal `4000` bit. A permission search with a leading minus matches files containing that bit alongside any ordinary mode.

---

In a symbolic mode, setuid belongs to the user class and is represented by `s`.

---

Use a symbolic subtraction so the ordinary read, write, and execute bits are preserved.
