A writable directory controls adding and removing names. The target file's write bit is not the deciding permission for deletion.

---

Look for a `t` in the others-execute position or a leading octal `1` in the directory mode.

---

Test the same delete operation against a file owned by another user and one created by your current identity.
