Expand every pathname component before deciding whether the final file or a parent directory is responsible.

---

The service already reaches the `data` directory. Compare the cache file's owner with the identity that must write it.

---

Change both named ownership fields in one operation, then run a non-outputting access check as the service user.
