Inspect the account's current authorized commands before creating a policy so the before and after states are explicit.

---

Ordinary shell redirection is opened by the unprivileged shell. Pipe the line to a writer that itself runs with elevated identity.

---

The rule must name the caller, root target, `NOPASSWD`, the absolute `systemctl` path, and the exact `restart app.service` arguments. Validate the file before trusting it.
