A `+` after the symbolic mode means the three traditional permission triplets are not the whole access model.

---

A named user ACL entry participates in the ACL group class and is limited by the mask entry.

---

Compare the permissions requested by `user:alice` with the intersection allowed by `mask`, then verify each operation independently.
