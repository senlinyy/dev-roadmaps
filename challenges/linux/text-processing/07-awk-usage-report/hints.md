Field expressions belong inside a single-quoted awk program so the shell leaves the dollar signs alone.

---

A numeric condition filters individual records. END runs once after reading the input and can print accumulated values.

---

An associative array can use the username as a bucket key. Sort the resulting rows explicitly rather than relying on array iteration order.
