The `-i` flag makes grep case-insensitive, and `-n` adds line numbers.

---

To count matches instead of printing them, use `grep -c "pattern" file`.

---

The `-v` flag inverts the match, showing only lines that do NOT contain the pattern.
