A pipeline normally reports its last command's status. A successful counter can hide an earlier failure.

---

The pipefail option uses the rightmost nonzero pipeline status. Read the status immediately, before another command replaces it.

---

A quiet grep returns one for a readable input with no match, without producing an error diagnostic.
