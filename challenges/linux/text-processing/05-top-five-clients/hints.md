Extract clients, group identical values, and count each group before applying a limit.

---

Use separate sort keys for descending counts and ascending client text. Applying the limit earlier changes the population being ranked.
