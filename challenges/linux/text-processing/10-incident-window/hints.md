Combine both time boundaries with a predicate on the status field. The end boundary is exclusive.

---

Save the selected raw records before deriving counts or rankings. Every later query should use that same population.

---

The endpoint and client occupy different columns. Group and count each independently, then keep the largest result.
