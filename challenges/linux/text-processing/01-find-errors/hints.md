Inspect a small sample from each end with the line-viewing tools. Severity is the second field.

---

An anchored expression can skip the timestamp and match only the severity field. Combine case-insensitive searching with numbered output.

---

Fixed-string mode keeps the dot in the request ID literal. A one-line context window retains the neighboring records.
