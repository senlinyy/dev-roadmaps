Counting adjacent duplicates is different from counting all occurrences. Group equal values before using the counting tool.

---

The final sort must use the count, not the status code. The output should contain exactly one row per distinct status.
