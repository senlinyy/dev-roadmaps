Running state and boot enablement are independent. Inspect both, even if you choose a combined activation operation.

---

The `--now` option combines enablement with current activation. A successful start alone does not configure future boots.
