The process settings belong in `[Service]`. The manager does not inherit your interactive shell's identity, current directory, or exports.

---

Loading a changed definition and launching the workload are separate operations. Focused `show` properties can verify the process contract.
