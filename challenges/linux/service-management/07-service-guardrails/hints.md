Put service resource settings in a `[Service]` drop-in. A manager configuration reload alone cannot replace an inherited process file limit.

---

Use `show` for cgroup configuration and the current main PID; inspect that PID's limits separately.
