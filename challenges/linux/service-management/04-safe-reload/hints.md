A supported application reload can adopt configuration without a stop-and-start cycle. Keep a backup before modifying the active candidate.

---

The Nginx configuration validator must accept the current saved file. A successful test of an earlier version is not sufficient in this lab.
