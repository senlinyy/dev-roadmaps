A successfully completed oneshot can be inactive. The timer, rather than the short-lived service process, should remain active and waiting.

---

The timer's calendar and persistence settings belong in `[Timer]`; its boot installation target belongs in `[Install]`.
