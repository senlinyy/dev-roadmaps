The first application error explains the loop; the final rate-limit message only explains why retries stopped. Filter out the previous boot.

---

Repair the environment before clearing failure state. A restart counter is evidence, not the underlying cause.
