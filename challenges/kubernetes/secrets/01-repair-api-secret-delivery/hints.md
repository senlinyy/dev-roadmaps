Author a container environment list where each public variable name receives its value through `valueFrom`, not a literal.

---

An explicit Secret reference identifies both the source object and one key inside it.
