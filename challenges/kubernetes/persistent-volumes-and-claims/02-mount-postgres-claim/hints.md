The Pod needs two matching halves: a Pod-level claim reference and a container-level mount. Build both lists in their proper scopes.

---

The volume name is the link between the Pod-level claim reference and the container-level mount.
