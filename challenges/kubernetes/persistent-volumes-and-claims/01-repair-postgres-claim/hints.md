The namespace belongs in `metadata`. Create a `spec` that expresses the storage profile, access requirement, and requested capacity as one PVC contract.

---

Use the production StorageClass explicitly instead of relying on a cluster default.
