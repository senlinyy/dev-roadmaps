Start with the workload relationship. The autoscaler needs a target reference that identifies the production Deployment.

---

Replica boundaries provide the availability floor and the dependency and cost ceiling.

---

CPU percentage scaling uses a Resource metric whose target is Utilization, measured against container CPU requests.
