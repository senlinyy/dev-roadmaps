The approved GatewayClass stays at the top of `spec`. Build the edge contract as one listener entry beside it.

---

The HTTPS listener owns both the TLS mode and its list of certificate references.

---

The same listener owns cross-namespace attachment. Use a namespace selector with a label matcher instead of a broad attachment mode.
