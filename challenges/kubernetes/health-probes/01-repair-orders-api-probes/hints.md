Map each endpoint to one platform decision: startup waits, readiness controls traffic, and liveness controls restarts.

---

The slow boot window is the probe period multiplied by its allowed failures.

---

Keep readiness and liveness timing explicit, but do not swap their endpoint meanings.
