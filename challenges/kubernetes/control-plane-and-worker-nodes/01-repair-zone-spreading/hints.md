The spread rule belongs in the Pod template spec because it guides placement of the Pods created by the Deployment.

---

The topology key names the node label whose values define the failure domains being balanced.

---

The label selector inside the constraint must identify the same `notification-api` Pods whose distribution is being measured.
