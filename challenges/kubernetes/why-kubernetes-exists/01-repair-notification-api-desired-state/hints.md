The stable object identity is already present. Rebuild the Deployment spec around the requested copy count, ownership selector, and Pod template.

---

The image is part of the first container in the Pod template, not a top-level Deployment field.
