The profile identity and driver are already correct. Add separate provider parameters, lifecycle controls, scheduling behavior, and metadata annotation to complete the production contract.

---

Provider parameters and annotations are strings here, while `allowVolumeExpansion` is a YAML boolean.
