The Namespace name, existing annotations, and new labels are sibling fields under `metadata`.

---

Create one labels mapping that identifies both the whole customer notification platform and the production environment.
