The ConfigMap identity is already approved. Add the sibling block that holds ordinary runtime key and string value pairs.

---

Quote values that the application must receive as strings, especially values that look numeric.
