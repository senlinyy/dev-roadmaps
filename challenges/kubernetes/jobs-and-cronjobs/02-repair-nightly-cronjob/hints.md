Create schedule, time zone, overlap, deadline, and history fields on the outer CronJob spec. Build retry and Pod behavior inside `jobTemplate`.

---

Trace the nesting from CronJob to Job template to Pod template before placing the cleanup container fields.
