Create a new claim rather than reusing the original PVC name. Build a `spec` that identifies the snapshot API, kind, and object name together.

---

The access mode and requested capacity remain normal PVC fields alongside the snapshot data source.
