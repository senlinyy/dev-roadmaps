The snapshot and source claim must share the application namespace. Correct the object identity, then create the nested source contract.

---

The PVC name belongs under `spec.source.persistentVolumeClaimName`.
