Start describe by binding let count = count_words(text);.
---
Both branches of the if need to produce a String.
---
Use String::from("No words") in the empty branch and format!("{count} words") in the other branch.
