The binary can import the library crate by package name: rust_notes::count_words.
---
The function body can use text.split_whitespace().count().
---
Do not move everything back into main.rs; the point is to keep reusable logic in lib.rs.
