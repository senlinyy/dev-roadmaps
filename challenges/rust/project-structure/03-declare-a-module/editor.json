{
  "language": "rust",
  "activeFile": "src/lib.rs",
  "files": [
    {
      "filename": "Cargo.toml",
      "initialValue": "[package]\nname = \"rust_notes\"\nversion = \"0.1.0\"\nedition = \"2021\"\n\n[dependencies]\n",
      "readOnly": true
    },
    {
      "filename": "src/lib.rs",
      "initialValue": "pub fn count_words(text: &str) -> usize {\n    todo!()\n}\n"
    },
    {
      "filename": "src/parser.rs",
      "initialValue": "pub fn words(text: &str) -> Vec<&str> {\n    text.split_whitespace().collect()\n}\n"
    },
    {
      "filename": "tests/module_test.rs",
      "initialValue": "use rust_notes::count_words;\n\n#[test]\nfn counts_words_through_parser() {\n    assert_eq!(count_words(\"module paths make code visible\"), 5);\n}\n",
      "readOnly": true
    }
  ],
  "executor": {
    "runtime": "rust-stable",
    "entrypoint": "Cargo.toml",
    "mode": "cargo-test",
    "runLabel": "Run Tests"
  },
  "grader": {
    "strategy": "execution",
    "expectedExitCode": 0,
    "output": [
      {
        "match": "contains",
        "value": "test result: ok",
        "label": "Cargo tests pass"
      }
    ]
  }
}
