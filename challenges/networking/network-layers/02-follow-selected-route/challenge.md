---
title: "Follow the Selected Route"
sectionSlug: which-tools-reveal-each-network-layer
order: 2
---

Public HTTPS uses the normal gateway, but inventory has a dedicated route. Establish the actual local routing decisions before blaming the default gateway. You start in `/home/dev`.

1. Inspect the route table.
2. Resolve the outgoing interface, source address, and next hop for `203.0.113.25`.
3. Resolve the same evidence for inventory at `10.40.8.20`, leaving the system unchanged.

The grader checks targeted observations and unchanged state, not copied text. This read-only IPv4 simulation has no live network access. Captures cover a bounded authored window; probes finish immediately.
